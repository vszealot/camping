<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="campspace">
<!-- 	<insert id="createBoard" parameterType="boardinfo">
		<![CDATA[INSERT INTO boardInfo VALUES(#{boardName}, #{nickname}, #{userId}, #{boardTag1}, #{boardTag2}, #{boardTag3}, #{boardTag4}, #{boardTag5})]]>
	</insert> -->

	<select id="campList" resultType="campinfo">
		<![CDATA[SELECT * FROM CAMPING_TEST]]>
	</select>
	
	<select id="campList" resultType="campinfo" parameterType="page">
		<![CDATA[SELECT * FROM (SELECT ROWNUM RN, AA.* FROM (SELECT *FROM CAMPING_TEST)AA)WHERE RN>=#{rowStart} AND RN<=#{rowEnd}]]>
	</select>

</mapper>
